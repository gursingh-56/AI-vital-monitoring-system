import t from"react";import r from"react-dom/client";import R from"./App";import"./index.css";const o=document.getElementById("root");if(!o)throw new Error("Could not find root element to mount to");const u=r.createRoot(o);u.render(<t.StrictMode>
    <R/>
  </t.StrictMode>);
