export const VITAL_RANGES={heartRate:{min:65,max:85},bloodSugar:{min:80,max:120},spo2:{min:98,max:100},temperature:{min:97.8,max:99}},getBloodPressureRanges=t=>t>=10&&t<=29?{systolic:{min:139,max:155},diastolic:{min:78,max:93}}:t>=30?{systolic:{min:120,max:142},diastolic:{min:72,max:88}}:{systolic:{min:110,max:125},diastolic:{min:70,max:85}};const n=[0,0,.15,.25,.15,0,0,-.2,1.8,-.4,0,0,.3,.45,.5,.45,.3,0,0,0];export const ECG_PATTERN_1=n.map(t=>t*20+50);const _=n.map((t,r)=>r===3?.4:r===8?2.8:r===14?.8:t);export const ECG_PATTERN_2=_.map(t=>t*20+50);const s=n.map((t,r)=>r===3?-.2:r>=12&&r<=16?t*-1.2:r===8?1:t);export const ECG_PATTERN_3=s.map(t=>t*20+50);const e=[-.4,2.8,-3.5,-1,.8,-1.5,-1.2,-.8,-.3,0],o=[...e,...Array(40-e.length).fill(0)];export const PVC_PATTERN_1=o.map(t=>t*15+50);const m=o.map((t,r)=>r>0&&r<4?t*1.2:t);export const PVC_PATTERN_2=m.map(t=>t*15+50);const E=o.map(t=>t*-.9);export const PVC_PATTERN_3=E.map(t=>t*15+50),ECG_DATA_LENGTH=150,ECG_TICK_MS=50,VITALS_TICK_MS=500,ECG_SAMPLE_RATE_HZ=1e3/ECG_TICK_MS,MONITORING_DURATION_MS=3e4,DEVICE_CONNECT_DURATION_MS=1800,HARDWARE_AVAILABLE=!1;
